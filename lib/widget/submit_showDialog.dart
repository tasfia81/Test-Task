import 'package:flutter/material.dart';

class SubmitShowdialog extends StatelessWidget {
  const SubmitShowdialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(

    );
    // return _showDialog(
    //   context: context,
    //   builder: (context) => Padding(padding: EdgeInsets.all(24),
    //     child: AlertDialog(
    //
    //     ),
    //   ),
    // );
  }
}
